services:
  db:
    ports:
      - "127.0.0.1:55433:5432"

  api:
    environment:
      NODE_ENV: development
      AUTH_MODE: dev
      DATA_PROVIDER: mock
      WEBHOOK_SECRET: ci-smoke-test-secret-0123456789
